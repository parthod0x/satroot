"""Packaging shim: exposes the runnable example ledgers and preset trees as
the `satroot_examples` package data inside built distributions. The canonical
files live in this directory; nothing imports Python code from here."""
