"""Packaging shim: exposes the protocol schemas and profile registry as the
`satroot_protocol` package data inside built distributions. The canonical
files live in this directory; nothing imports Python code from here."""
